# Copy to Infra/.env and adjust. Compose loads .env from this directory automatically.

# Host directory holding the llama.cpp GGUF files referenced by llama/config/models.ini.
# Defaults to ./models when unset. Use forward slashes on Windows.
GGUF_MODELS_DIR=D:/Dev/LLMModels
